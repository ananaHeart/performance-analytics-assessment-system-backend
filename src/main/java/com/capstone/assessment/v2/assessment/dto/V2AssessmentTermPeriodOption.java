package com.capstone.assessment.v2.assessment.dto;

public record V2AssessmentTermPeriodOption(
        Integer termPeriodId,
        String termName,
        Integer termOrder,
        String status
) {
}
